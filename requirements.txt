# Loose spec. The exact, hashed versions that Docker, CI and `make setup` install live in
# requirements.lock (`make lock` regenerates it). Upper bounds guard the APIs the code
# relies on (Starlette 1.x TestClient, APScheduler 3.x BackgroundScheduler).

# Runtime
fastapi>=0.115,<1
starlette<2
uvicorn[standard]>=0.30,<1
sqlalchemy>=2.0,<3
jinja2>=3.1,<4
pydantic-settings>=2.4,<3
python-multipart>=0.0.9,<1
httpx>=0.27,<1
itsdangerous>=2.2,<3
apscheduler>=3.10,<4
tzdata>=2024.1

# Dev / test
pytest>=8,<10
ruff>=0.6,<1
